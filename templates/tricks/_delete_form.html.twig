<form method="post" style="display:inline" action="{{ path('app_tricks_delete', {'id': trick.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ trick.id) }}">
    <button class="btn"><i class="bi bi-trash3" style="color: red"></i></button>
</form>
